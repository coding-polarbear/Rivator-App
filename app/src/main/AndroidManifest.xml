<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rinc.bong.rivatorproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignupDegreeActivity" android:windowSoftInputMode="adjustResize|stateHidden"/>
        <activity android:name=".SignupPortfolioActivity" android:windowSoftInputMode="adjustResize|stateHidden"/>
        <activity android:name=".SignupTermsAgreeActivity"  />
        <activity android:name=".SignupTermsDetailActivity"  />
        <activity android:name=".SelectActivity" />
        <activity android:name=".TeacherSignUpActivity" />
        <activity android:name=".StudentSignUpActivity"></activity>
    </application>

</manifest>